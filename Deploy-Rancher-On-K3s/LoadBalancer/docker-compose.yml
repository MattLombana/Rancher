version: '3.7'
services:
  nginx:
    image: nginx:1.14
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    labels:
      - "loadbalancer"
